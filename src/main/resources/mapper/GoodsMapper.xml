<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.ramsample.testProject.Mapper.GoodsMapper">

    <select id="selectGoodsByGoodsId" resultType="Goods">
		SELECT GOODSNO , GOODSNAME, GOODSPRICE , RMK
  		  FROM GOODS
		WHERE GOODSNO = 1
    </select>

    <insert id="insertGoods" >    
        <selectKey keyProperty="goodsNo" resultType="int" order="BEFORE">        
            SELECT NVL(MAX(GOODSNO + 1 ),1) FROM GOODS
        </selectKey> /* 상품 Insert */        
 			INSERT INTO GOODS (GOODSNO , GOODSNAME, GOODSPRICE, RMK ,INPT_ID , INPT_DT , UPDT_ID , UPDT_DT)
 			VALUES (#{goodsNo} , #{goodsName} , #{goodsPrice} , #{rmk} , #{inpt_id} , SYSDATE , #{updt_id} , SYSDATE )        
    </insert>

</mapper>